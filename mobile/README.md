# mobile/ — App Lupa (Capacitor)

Casca nativa iOS/Android que empacota o web app (modo **remote**, `server.url` = domínio do
app / CloudFront). Build e entrega via **Codemagic**, disparado por Git. Push via **Amazon SNS**
(APNs/FCM). Ver `../docs/MOBILE_CAPACITOR.md` e `../docs/GIT_CICD.md` §4.

> Stub por ora. O scaffold Capacitor (`capacitor.config.ts`, `ios/`, `android/`, ícone/splash,
> `codemagic.yaml`) é criado no **prompt 09 / Bloco 8**.
