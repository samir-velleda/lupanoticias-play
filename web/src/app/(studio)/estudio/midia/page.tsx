import { EmBreve } from '@/components/portal/EmBreve';

export default function EstudioMidiaShell() {
  return (
    <EmBreve
      titulo="Biblioteca de mídia"
      descricao="Lista de vídeos/podcasts com status (enviando/processando/pronto/erro). Chega no Bloco 6."
    />
  );
}
